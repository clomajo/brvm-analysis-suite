name: BRVM Analysis Suite V25.0 - Mistral AI

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  brvm-analysis:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    
    steps:
      - name: 🔥 Checkout
        uses: actions/checkout@v4
      
      - name: 🐍 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Installation dépendances
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 🌐 Installation Chrome/Chromedriver
        run: |
          sudo apt-get update
          sudo apt-get install -y wget unzip
          wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          sudo apt-get install -y ./google-chrome-stable_current_amd64.deb
          
          CHROME_VERSION=$(google-chrome --version | grep -oP '\d+\.\d+\.\d+')
          CHROME_MAJOR_VERSION=$(echo $CHROME_VERSION | cut -d. -f1)
          
          wget -q "https://storage.googleapis.com/chrome-for-testing-public/${CHROME_VERSION}.0/linux64/chromedriver-linux64.zip" -O chromedriver.zip || \
          wget -q "https://chromedriver.storage.googleapis.com/LATEST_RELEASE_${CHROME_MAJOR_VERSION}" -O version.txt && \
          CHROMEDRIVER_VERSION=$(cat version.txt) && \
          wget -q "https://chromedriver.storage.googleapis.com/${CHROMEDRIVER_VERSION}/chromedriver_linux64.zip" -O chromedriver.zip
          
          unzip -q chromedriver.zip
          if [ -d "chromedriver-linux64" ]; then
            sudo mv chromedriver-linux64/chromedriver /usr/local/bin/
          else
            sudo mv chromedriver /usr/local/bin/
          fi
          sudo chmod +x /usr/local/bin/chromedriver
          
          rm -rf chromedriver-linux64 chromedriver.zip version.txt google-chrome-stable_current_amd64.deb
          
          echo "✅ Chrome: $(google-chrome --version)"
          echo "✅ Chromedriver: $(chromedriver --version)"
      

      - name: 📈 ÉTAPE 0 - Update Index Values
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "🚀 Updating BRVMC and BRVM30 index values..."
          python update_index.py
          echo "✅ Index updated"

      - name: 📊 ÉTAPE 1 - Collecte Données (Supabase)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "🚀 Collecte données BRVM..."
          python data_collector_simple.py
          echo "📄 Bulletin Officiel de la Cote - PER/Dividende/Rdt.Net..."
          python scrape_boc_pdf.py
          echo "📈 Mise à jour indices BRVMC + BRVM30..."
          python scrape_indices.py
          echo "📦 Mise à jour commodités..."
          python scrape_commodities.py
          echo "✅ Collecte terminée"
      

      - name: 📊 ÉTAPE 1b - Scrape Fondamentaux (hebdomadaire)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "🚀 Scraping fondamentaux depuis stockanalysis.com..."
          # Tourne seulement le lundi (jour 1)
          DAY=$(date +%u)
          if [ "$DAY" = "1" ]; then
            python scrape_all_v4.py --full
            echo "✅ Fondamentaux mis à jour"
          else
            echo "⏭️ Skipped — tourne seulement le lundi"
          fi

      - name: 📅 ÉTAPE 1c - Scrape Corporate Events (hebdomadaire)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "🚀 Scraping corporate events depuis Sikafinance..."
          DAY=$(date +%u)
          if [ "$DAY" = "1" ]; then
            echo "o" | python scrape_corporate_events.py
            echo "✅ Corporate events mis à jour"
          else
            echo "⏭️ Skipped — tourne seulement le lundi"
          fi

      - name: 🎯 ÉTAPE 1f - Cours Cibles V2 (target_prices)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "🎯 Calcul cours cibles V2..."
          python calculate_target_price.py
          echo "✅ Cours cibles mis à jour"

      - name: 🧪 ÉTAPE 1g - Fair Value V3 (target_prices_v3 — expérimental)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        continue-on-error: true
        run: |
          echo "🧪 Calcul Fair Value V3 (table dédiée, n'écrase pas V2)..."
          python calculate_target_price_v3.py --apply
          echo "✅ Fair Value V3 calculée"

      - name: 📈 ÉTAPE 2 - Analyse Technique (Supabase)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "🚀 Analyse technique..."
          python technical_analyzer_simple.py
          echo "✅ Analyse technique terminée"
      
      - name: 🏆 ÉTAPE 3 - Opportunity Scorer
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "🚀 Calcul des scores d'opportunités..."
          python opportunity_scorer_simple.py
          echo "✅ Scores mis à jour"
      

      - name: 🎯 ÉTAPE 3b - Generate Decisions
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "🚀 Generating daily decisions..."
          python generate_decisions.py
          echo "✅ Decisions generated"

      - name: ✅ ÉTAPE 3c - Verify Decisions (J+20 — ADR-019)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}
        run: |
          echo "🚀 Verifying signals from 20 days ago (J+20)..."
          python verify_decisions.py
          echo "✅ Verification complete"

      - name: 🔍 ÉTAPE 3e - Verify GRU Predictions
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "🚀 Verifying GRU predictions vs actual prices..."
          if python verify_predictions.py; then
            echo "✅ Prediction verification complete"
          else
            echo "⚠️ Échec verification — non bloquant"
          fi

      - name: 🧪 ÉTAPE 3d - Tests Qualité Pipeline
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "🔍 Tests qualité pipeline..."
          python test_pipeline.py
          echo "✅ Tests qualité validés"

      - name: 🔮 ÉTAPE 4 - Prédictions GRU (Supabase REST)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "🚀 Prédictions GRU 10 jours..."
          if python prediction_analyzer_v2.py; then
            echo "✅ Prédictions générées"
          else
            echo "⚠️ Échec prédictions — non bloquant"
          fi
      
      - name: 📄 ÉTAPE 5 - Analyse Fondamentale (Mistral AI — bi-hebdomadaire)
        env:
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
          MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}
        run: |
          # Bi-hebdomadaire : le 1er et le 15 du mois uniquement.
          # Les fondamentaux changent au rythme des publications de rapports
          # (trimestriel au mieux), donc une analyse quotidienne consommait
          # inutilement le quota API Mistral (cf. session du 28/06/2026).
          DOM=$(date +%d)
          if [ "$DOM" = "01" ] || [ "$DOM" = "15" ]; then
            echo "🚀 Analyse fondamentale avec Mistral AI..."
            echo "🤖 Modèle: mistral-large-latest"
            python fundamental_analyzer.py
            echo "✅ Analyse fondamentale terminée"
          else
            echo "⏭️ Skipped — analyse fondamentale bi-hebdomadaire (1er et 15 du mois)"
          fi
      
      - name: 📝 ÉTAPE 6 - Génération Rapports (Mistral AI — bi-hebdomadaire)
        env:
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
          MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}
        run: |
          # Bi-hebdomadaire (1er et 15), aligné sur l'ÉTAPE 5 : les rapports
          # sont générés à partir des analyses fondamentales, qui ne changent
          # que deux fois par mois — inutile de régénérer quotidiennement
          # (économie de quota Mistral, cf. session du 28/06/2026).
          DOM=$(date +%d)
          if [ "$DOM" = "01" ] || [ "$DOM" = "15" ]; then
            echo "🚀 Génération rapports avec Mistral AI..."
            if python report_generator.py; then
              echo "✅ Rapports générés"
            else
              echo "⚠️ Échec génération rapports — non bloquant"
            fi
          else
            echo "⏭️ Skipped — génération rapports bi-hebdomadaire (1er et 15 du mois)"
          fi
      
      - name: 📰 ÉTAPE 7 - Collecte News & Scoring IA
        env:
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "📰 Collecte des news BRVM..."
          if python news_collector.py; then
            echo "✅ News collectées"
          else
            echo "⚠️ Échec collecte news — non bloquant"
          fi
  

      - name: 📡 ÉTAPE V2 - Signaux Actifs J-10 (hebdomadaire)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "📡 Calcul signaux actifs V2..."
          DAY=$(date +%u)
          if [ "$DAY" = "1" ]; then
            python signaux_actifs.py
            echo "✅ Signaux V2 calculés"
          else
            echo "⏭️ Skipped — tourne seulement le lundi"
          fi

      - name: 📊 ÉTAPE V2b - Market Cap (mensuel — 1er lundi du mois)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          echo "📊 Mise à jour market cap..."
          DAY=$(date +%u)
          DOM=$(date +%d)
          if [ "$DAY" = "1" ] && [ "$DOM" -le 7 ]; then
            python scrape_market_cap.py
            echo "✅ Market cap mis à jour"
          else
            echo "⏭️ Skipped — tourne seulement le 1er lundi du mois"
          fi

      - name: 📤 Upload Rapports
        uses: actions/upload-artifact@v4
        with:
          name: rapports-brvm-${{ github.run_number }}
          path: Rapport_*.docx
          retention-days: 90
      
      - name: 📊 Résumé
        if: always()
        run: |
          echo "═══════════════════════════════════════════════════════════"
          echo "📈 BRVM ANALYSIS SUITE V25.0 (Mistral AI)"
          echo "═══════════════════════════════════════════════════════════"
          echo "Date: $(date '+%Y-%m-%d %H:%M:%S UTC')"
          echo "Run: ${{ github.run_number }}"
          echo ""
          echo "✅ Pipeline terminé"
          echo "═══════════════════════════════════════════════════════════"
